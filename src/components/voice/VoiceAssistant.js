import React from "react";

export default function VoiceAssistant({ user }) {
  return (
    <div>
      <h2>Voice Assistant for {user.username}</h2>
      <p>This feature is coming soon.</p>
    </div>
  );
}